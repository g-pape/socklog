#!/bin/sh

LOGDIR=/var/log/socklog-remote

exec 2>&1
exec setuidgid log multilog t ${LOGDIR}/all

# not reached

# use this f.e. to separate log by client
exec 2>&1
exec setuidgid log multilog t ${LOGDIR}/all \
  -* +'* 10.0.0.236:*' ${LOGDIR}/10.0.0.236
